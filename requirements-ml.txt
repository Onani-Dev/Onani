deepdanbooru>=1.0.4
# CPU-only TensorFlow for Docker/prod images (no ROCm or CUDA stack required).
# tensorflow-rocm requires libhsa-runtime64 (AMD ROCm) at import time which
# is not present in production containers.
tensorflow-cpu>=2.14,<2.15
tensorflow-io>=0.34.0,<0.38.0
