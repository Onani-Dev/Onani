deepdanbooru>=1.0.4
# Model files are saved in Keras 3 format (.keras with keras.src.* module paths).
# Keras 3 is bundled with TensorFlow >= 2.16; do not use an older version.
# tensorflow-rocm was a failed experiment and should not be used in containers.
tensorflow-cpu>=2.16
tensorflow-io>=0.34.0,<0.38.0
